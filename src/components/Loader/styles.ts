import {createUseStyles} from '../Theme';

export default createUseStyles({
  wrapper: {
    position: 'relative',
    margin: 0,
  },
  circular: {
    width: 48,
    height: 48,
  },
  bar: {
    width: 240,
    height: 4,
  },
});
