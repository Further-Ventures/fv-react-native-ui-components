import {createUseStyles} from '../Theme';

export default createUseStyles({
  wrapper: {
    position: 'relative',
    margin: '0 auto',
  },
  circular: {
    width: '48px',
    height: '48px',
  },
  bar: {
    width: '240px',
    height: '4px',
  },
});
