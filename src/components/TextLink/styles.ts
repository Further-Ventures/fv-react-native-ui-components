import { createUseStyles } from '../Theme';

export default createUseStyles((theme) => ({
  underline: {
    textDecorationLine: 'underline',
  },
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
}));
